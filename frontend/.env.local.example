# Variables de entorno para Next.js (Desarrollo local)
# Este archivo NO debe subirse a Git - solo para desarrollo local

# URL del backend API (accesible desde el navegador)
# En Docker: usar el nombre del servicio 'backend'
# En desarrollo local: usar localhost
NEXT_PUBLIC_API_URL=http://localhost:8000

# Otras variables públicas del frontend (prefijo NEXT_PUBLIC_)
# NEXT_PUBLIC_SITE_NAME=Proyecto IA Aplicada
# NEXT_PUBLIC_SITE_URL=http://localhost:4200

# Variables privadas del servidor (NO se exponen al cliente)
# API_SECRET_KEY=your-secret-key-here
